# Expire-bot runtime dependencies
#
# Пины выбраны как "текущее стабильное + явный upper bound, чтобы апгрейд
# был осознанным". Обновляй при выпуске новой мажорной версии matrix-nio
# (там бывают breaking changes в API).
#
# matrix-nio:    https://github.com/poljar/matrix-nio   (последний 0.25.x)
# aiosqlite:     https://github.com/omnilib/aiosqlite    (0.22.x)
# aiohttp:       https://github.com/aio-libs/aiohttp     (3.9.x LTS)
# pyyaml:        https://github.com/yaml/pyyaml          (6.0.x)

matrix-nio[e2e]>=0.25.0,<0.27.0
aiosqlite>=0.20.0,<1.0.0
aiohttp>=3.9.0,<4.0.0
PyYAML>=6.0,<7.0
